import React from "react";

function Loading() {
  return (
    <div
      style={{
        display: "grid",
        placeItems: "center",
        width: "100vw",
        height: "100vh",
      }}
    >
      <h1>Loading...</h1>
    </div>
  );
}

export default Loading;
